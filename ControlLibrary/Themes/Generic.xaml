<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ControlLibrary">
    <!--Source="{TemplateBinding ImageSource}"-->
    <Style TargetType="{x:Type local:MovementControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MovementControl}">
                    <Canvas>
                        <Image Name="PART_Image" Source="{TemplateBinding ImageSource}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Canvas.Left="{TemplateBinding X}" Canvas.Top="{TemplateBinding Y}" Stretch="Uniform">
                        </Image>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
